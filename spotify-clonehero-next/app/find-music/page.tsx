import FindMusicClient from './FindMusicClient';

export const metadata = {
  title: 'Find Music',
  description:
    'Find Chorus charts for music in your library and listening history, with private on-device recommendations.',
};

export default function FindMusicPage() {
  return <FindMusicClient />;
}
